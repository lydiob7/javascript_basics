/* ======================= Datos primitivos ========================= */

const miNumero = 89;
const miCadenaDeTexto = "Esto es un string";
const otroString = "Esto tambien es valido";
const tercerString = `Esto tambien`;
const miBooleano = true;
const miNull = null;
const miUndefined = undefined;
const miBigInt = 9089419023840912873490n;
const miSimbolo = Symbol();

/* ======================= Datos complejos ========================= */

const miObjeto = {
  color: "Rojo",
  altura: 88,
  ancho: 12,
  direccion: {
    calle: "algo",
    numer: 123,
  },
};

const miArreglo = [1, 2, 3, 4, "Hola", { a: "b" }, miObjeto];

/* ======================= Strings (cadenas de texto) ========================= */

const cadenaDeTexto = 'Utilizar una frase "Esta es la frase" ';
const nombre = "Juan";
const apellido = "Perex";
const descripcion = `El nombre de esta persona es: ${nombre} ${apellido}`;
